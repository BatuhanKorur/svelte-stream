{
  "name": "fuu-socket",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "dev:server": "bun run --watch src/server.ts"
  },
  "dependencies": {
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@types/bun": "^1.2.4"
  }
}